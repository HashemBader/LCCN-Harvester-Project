# Configuration Overview

## Purpose
Describe where configuration lives, what files are used, and how settings persist between runs.

## Content (to fill)
- targets file
- preferences (call-number type, retry days, API toggles)
- default values
